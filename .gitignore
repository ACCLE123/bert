# 模型权重 (体积最大，绝对不要上传)
model/
**/*.bin
**/*.pt
**/*.pth
**/*.ckpt
**/*.safetensors

# 数据库文件 (体积巨大，绝对不要上传)
**/*.db
**/*.db-journal
**/*.sqlite

# 数据文件 (通常很大，建议只上传脚本或小样本地数据)
**/*.csv
**/*.jsonl
**/*.parquet

# 训练日志和输出
logs/
runs/
checkpoint-*/

# Python 缓存和环境
__pycache__/
*.py[cod]
*$py.class
venv/
.env
.venv
env/
build/
dist/
*.egg-info/

# IDE 和系统文件
.DS_Store
.vscode/
.cursor/
.idea/

# 临时提取的特征缓存 (如果有的话)
**/*.npy
**/*.npz
